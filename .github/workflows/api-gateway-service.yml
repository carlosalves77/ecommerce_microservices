name: CI/CD Homelab - API Gateway

on:
  push:
    branches: [ "master" ]
    paths:
      - 'api-gateway/**'  

jobs:
  build-and-deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      - name: Ler versão do pom.xml
        id: get_version
        run: |
          cd api-gateway

          SHORT_SHA=$(git rev-parse --short HEAD)
          VERSION=$(mvn help:evaluate -Dexpression=project.version -q -DforceStdout)
          FINAL_VERSION="${VERSION}-${SHORT_SHA}"
          echo "PROJECT_VERSION=$FINAL_VERSION" >> $GITHUB_ENV
          
          echo "Versão detectada no POM: $VERSION"
          
          echo "PROJECT_VERSION=$VERSION" >> $GITHUB_ENV
      - name: Build com Maven
        run: mvn clean package -DskipTests
        working-directory: ./api-gateway

      - name: Login no Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build e Push da Imagem
        run: |
          cd api-gateway
          
          echo "Buildando imagem versão: $PROJECT_VERSION"
          
          # Usa a versão extraída do POM ($PROJECT_VERSION)
          docker build -t ${{ secrets.DOCKER_USERNAME }}/api-gateway:$PROJECT_VERSION .
          docker push ${{ secrets.DOCKER_USERNAME }}/api-gateway:$PROJECT_VERSION
   
      - name: Atualizar Repositório de CD (GitOps)
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }} 
          USER_EMAIL: "ci-bot@outlook.com"
          USER_NAME: "CI Bot"
          CD_REPO: "carlosalves77/homelab-kubernetes" 
          FILE_PATH: "api-gateway-k8s/api-gateway-deploy.yaml"
        run: |
          echo "Clonando repositório de CD..."

          git config --global --unset credential.helper || true
          
          git clone https://${GH_TOKEN}@github.com/${CD_REPO}.git cd-repo

          cd cd-repo
          
          # Configurar Git
          git config user.name "$USER_NAME"
          git config user.email "$USER_EMAIL"

          echo "Tentando editar: $FILE_PATH"
          ls -la $FILE_PATH || echo "ERRO: Arquivo não encontrado!"

          sed -i "s|image: ${{ secrets.DOCKER_USERNAME }}/api-gateway:.*|image: ${{ secrets.DOCKER_USERNAME }}/api-gateway:$PROJECT_VERSION|" $FILE_PATH

          if git diff --quiet; then
            echo "Nenhuma mudança detectada no manifesto Kubernetes. A versão já está atualizada?"
          else
            echo "Mudança detectada. Commitando..."
            git add $FILE_PATH
            git commit -m "Update api-gateway image to version $PROJECT_VERSION"
            git push origin main 
            echo "Repositório de CD atualizado com sucesso!"
          fi
